import React from "react";

export default function BoxService2(props) {
  return (
    <div className="col-md BoxService2">
      <h1>{props.heading}</h1>
      <p>{props.about}</p>
    </div>
  );
}

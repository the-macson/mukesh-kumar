import React from "react";

export default function BoxService2(props) {
  return (
    <div className="col-lg-6 BoxService2">
      <h1>{props.heading}</h1>
      <p>{props.about}</p>
    </div>
  );
}
